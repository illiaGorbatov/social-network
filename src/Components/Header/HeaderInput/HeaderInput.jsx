import React from "react";
import style from "./HeaderInput.module.sass";
import {NavLink} from "react-router-dom";

const HeaderInput = () => {
    return (
        <div className={style.headerInput}>

        </div>
    )
};

export default HeaderInput;